/**
 * Colors Hook — Athletly V2
 *
 * Returns the design system color tokens.
 * Single source of truth for all color references in components.
 */

import { Colors } from '@/lib/colors';

export function useColors() {
  return Colors;
}
